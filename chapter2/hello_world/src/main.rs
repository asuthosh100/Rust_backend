// --------------------------------STEP 1 : Project Setup--------------------------------

use std::sync::mpsc;
use std::sync:Arc; 
use std::thread;
use std::time::Duration;

fn main() {
    println!("Hello, world!");
}
